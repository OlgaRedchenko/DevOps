# бла бла

FROM nginx:latest 

<h1>Используем официальный образ Nginx</h1> 
FROM nginx:latest 
<h1>Копируем конфигурационный файл Nginx</h1> 
COPY nginx.conf /etc/nginx/conf.d/default.conf 
<h1>Копируем сайт в папку /usr/share/nginx/html</h1> 
COPY ./site /usr/share/nginx/html 
<h1>Выставляем порт 80 для доступа к сайту</h1> 
EXPOSE 80 
<h1>Запускаем Nginx</h1> 
CMD ["nginx", "-g", "daemon off;"]

location /somepath {
    limit_except GET {
        deny all;
    }
}

nginx.conf
